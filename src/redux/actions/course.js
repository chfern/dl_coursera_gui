import {
  UPDATE_COURSE_ROOT_DIR
} from './types'
// const fs = require('fs');

export const updateCourseRootDir = (courseRootDir) => dispatch => {
  // fs.readdir(courseRootDir, (err, files) => {
  //   files.forEach(file => {
  //     console.log(file);
  //   });
  //   dispatch({ type: UPDATE_COURSE_ROOT_DIR, courseRootDir: null})
  // });
}